.venv/
__pycache__/
.pytest_cache/
.env
# Salida de `sam build` y configuración de `sam deploy --guided` (tiene los
# secretos en texto plano).
.aws-sam/
samconfig.toml
# El .gitignore de la raíz ignora `.env*`; la plantilla sí se versiona.
!.env.example
